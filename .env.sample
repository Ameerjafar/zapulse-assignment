BACKEND_PORT=5000
DATABASE_URL=""
# DATABASE_URL options
#
# Option 1 - Use your own PostgreSQL (recommended for production):
#  Set `DATABASE_URL` to your hosted database connection string, for example:
#  postgresql://user:password@db-host:5432/mydb?schema=public
#
# Option 2 - Use the local Postgres provided by `docker-compose.yml` (for local development)
# copy the link blow into the DATABASE_URL value
# postgresql://zapulse:zapulse_pwd@localhost:5432/zapulse_db?schema=public

# NOTE: Provide one `DATABASE_URL` value in your `.env` file. If you keep this file
# as-is, copy one of the example lines above into a new `.env` and remove the leading `#`.